import {
  BASE_URL,
  BOT_TOKEN,
  BOT_USERNAME,
  DESCRIPTION,
  PROJECT_NAME,
  VERSION,
} from "./env";

export const settings = {
  token: BOT_TOKEN,
  username: BOT_USERNAME,
  version: VERSION,
  description: DESCRIPTION,
  name: PROJECT_NAME,
  base_url: BASE_URL,
};
